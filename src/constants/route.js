const PAGE_CHANGE_TYPE = {
  PUSH: 'push',
  REPLACE: 'replace',
};

const PAGE_CHANGE_EVENT_NAME = 'route-change';

Object.freeze(PAGE_CHANGE_TYPE);
Object.freeze(PAGE_CHANGE_EVENT_NAME);
export { PAGE_CHANGE_EVENT_NAME, PAGE_CHANGE_TYPE };
